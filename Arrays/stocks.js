/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
  let minprice = prices[0]
   for(let i = 0; i<prices.length;i++){
      if(arr[i]<minprice){
      minprice = arr[i];
    }
  }
  for(let )
};
